name: tests-macos

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v4

    - name: install
      run: make install

    - name: testing
      run: make tests

    - name: Run check
      run: make check

    - name: Run distcheck
      run: make distcheck
